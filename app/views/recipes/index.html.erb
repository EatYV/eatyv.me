<% if !user_signed_in? %>
  <div class="home_background">
    <div class="home_background-content">
      <h1>EatYV</h1>
      <p>
        Create your own recipes or eat Your Vegetables!
      </p>
      <%= link_to 'Get Started', new_user_registration_path, class: 'button warning expanded'  %>
      <%= link_to 'Sign In', new_user_session_path, class: 'button warning expanded' %>
    </div>
  </div>
<% end %>
<!--
<div class="white_background">
  <h1>Recipes</h1>
  <br>

  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Price</th>
        <th>Duration</th>
        <th>Description</th>
        <th>Image</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @recipes.each do |recipe| %>
        <tr>
          <td><%= recipe.title %></td>
          <td><%= recipe.price %></td>
          <td><%= recipe.duration %></td>
          <td><%= recipe.description %></td>
          <td><%= image_tag recipe.image_url(:thumb) %></td>
          <td><%= link_to 'Show', recipe %></td>

          <% if current_user == recipe.user %>
            <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
            <td><%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <% if user_signed_in? %>
    <%= link_to 'New Recipe', new_recipe_path, class: 'button warning' %>
  <% end %>
</div>
-->
<div class="white_background">
<div class="featured-image-block-grid">
  <div class="featured-image-block-grid-header small-10 medium-8 large-7 columns text-center">
    <h1>Recipes</h1>
  </div>
  <div class="row large-up-4 small-up-2">
    <div class="featured-image-block column">
    <% @recipes.each do |recipe| %>
     <%= link_to 'show', recipe, class: 'hollow button success' %>
     <% if current_user == recipe.user %>
            <%= link_to 'Edit', edit_recipe_path(recipe), class: 'hollow button secondary ' %>
            <%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' }, class: 'hollow button alert' %>
          <% end %>
        <%= image_tag recipe.image_url(:thumb) %>
        <p class="text-center featured-image-block-title"><%= recipe.title %></p>
        <p class="text-center">Priece: <%= recipe.price %>
        <br><br>Cooking time: <%= recipe.duration %> (min)
        <br><br><%= recipe.description %></p> <!-- fuge textul...trebei facut ceva -->
      <%end%>
    </div>
  </div>
</div>
</div>

